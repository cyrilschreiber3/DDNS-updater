# Dynamic DNS Updater 

This is a simple Python script to update my dynamic DNS records for my domains. It is intended to be run as a cron job.
I also created a simple web interface with [Nuxt 3](https://v3.nuxtjs.org/) to add/remove domains and run the script manualy.